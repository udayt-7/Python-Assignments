#Write a program find-large-files.py to find files recursively in a directory tree that are larger than given size. 
##The program should accept the directory and the size as command-line argument. 
###The size can be also be specified with K, M and G suffix for KB, MB and GB respectively.

'''import file1
import os
import sys
import cat

d_name = file1.get_filename()
s = file1.get_filename2()


import sys
import os

def get_size(st):
	if gsize[-1] == 'K':
		return int(st[:-1])*1024
	elif gsize[-1] == 'M':
		return int(st:-1])*1024*1024
	elif gsize[-1] == 'G':
		return int(st[:-1])*1024*1024*1024
	else:
		return int(st)
'''


